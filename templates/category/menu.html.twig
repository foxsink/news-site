<div class="d-flex flex-column w-100">
    {% for branch in categoryArray %}
        <div class="d-flex flex-column w-100">
        {% for category in branch %}
            {{ category.name }}
        {% endfor %}
        </div>
    {% endfor %}
</div>